import{getApps,getApp}from'https://www.gstatic.com/firebasejs/10.7.0/firebase-app.js';
import{getAuth}from'https://www.gstatic.com/firebasejs/10.7.0/firebase-auth.js';
import{getFirestore,collection,doc,getDocs,query,orderBy,onSnapshot,updateDoc,addDoc,setDoc,serverTimestamp}from'https://www.gstatic.com/firebasejs/10.7.0/firebase-firestore.js';
import{GOOGLE_CLIENT_ID,DRIVE_SCOPE}from'./drive-config.js';
const app=getApps().length?getApp():null;if(!app)return;const auth=getAuth(app),db=getFirestore(app);let uid=localStorage.getItem('uid'),nickname=localStorage.getItem('nickname');
const $=id=>document.getElementById(id);const chatId=(a,b)=>[a,b].sort().join('_');let users=[],path=null,items=[],unsub=null,pendingReply=null,pendingReplyAt=0,oldTexts=new Map(),hidden=new Set(),tokenClient=null,accessToken=null;
const emojis=['❤️','😂','👍','😮','😢','🔥'];
function esc(s){return String(s??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function currentHidden(){try{return new Set(JSON.parse(localStorage.getItem('tanmira-hidden-'+uid+'-'+path)||'[]'))}catch{return new Set()}}
function saveHidden(){localStorage.setItem('tanmira-hidden-'+uid+'-'+path,JSON.stringify([...hidden]))}
async function getPath(){const title=($('title')?.textContent||'').trim();if(!title||title==='Everyone')return'global';if(!users.length){const s=await getDocs(collection(db,'users'));users=s.docs.map(d=>d.data())}const u=users.find(x=>x.nickname?.trim().toLowerCase()===title.toLowerCase());return u?chatId(uid,u.uid):null}
function inject(){if(document.getElementById('featureStyle'))return;const s=document.createElement('style');s.id='featureStyle';s.textContent=`.featureTools{display:flex;gap:5px;flex-wrap:wrap;margin-top:4px;opacity:.78}.featureTools button{border:0;background:transparent;color:#9ca8bd;font-size:11px;padding:2px 5px;border-radius:7px}.featureTools button:hover{background:#273143;color:#fff}.reactionRow{display:flex;gap:3px;flex-wrap:wrap;margin-top:5px}.reactionChip{border:1px solid #fff2;background:#141b28;color:#e9edf8;border-radius:12px;padding:2px 7px;font-size:11px;cursor:pointer}.replyQuote{border-left:3px solid var(--accent);background:#0002;border-radius:7px;padding:5px 8px;margin-bottom:6px;font-size:11px;color:#aeb9cf}.replyQuote b{display:block;color:#dce3f5;margin-bottom:2px}.deletedMsg{font-style:italic;opacity:.65}.pinnedBar{display:none;padding:8px 12px;background:var(--panel);border-bottom:1px solid #fff1;font-size:12px;color:#cbd4e7;cursor:pointer}.pinnedBar.show{display:block}.pinnedBar b{color:var(--accent)}.featureSearchBtn{margin-left:auto}.replyBanner{display:none;align-items:center;gap:8px;padding:7px 12px;background:var(--panel);border-top:1px solid #fff1;font-size:12px}.replyBanner.show{display:flex}.replyBanner span{flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.replyBanner button{border:0;background:transparent;color:#fff;font-size:15px}.voiceBtn.recording{background:#c94b5d!important}.voiceBubble{display:flex;align-items:center;gap:8px;min-width:190px}.voiceBubble audio{width:190px;max-width:100%}.historyModal{position:fixed;inset:0;background:#0009;display:none;place-items:center;z-index:120}.historyModal.open{display:grid}.historyCard{width:min(440px,92vw);max-height:80dvh;overflow:auto;background:#151c29;border:1px solid #fff2;border-radius:16px;padding:16px}.historyItem{padding:9px 0;border-bottom:1px solid #fff1;color:#cbd3e3;font-size:12px}.historyItem small{display:block;color:#7f8aa0;margin-bottom:4px}`;document.head.appendChild(s)}
function ensureUI(){const top=$('topAvatar')?.parentElement?.parentElement;if(top&&!$('featureSearchBtn')){const b=document.createElement('button');b.id='featureSearchBtn';b.className='iconBtn featureSearchBtn';b.textContent='🔎 Messages';b.onclick=searchMessages;top.appendChild(b)}const msgs=$('messages');if(msgs&&!$('pinnedBar')){const p=document.createElement('div');p.id='pinnedBar';p.className='pinnedBar';msgs.parentElement.insertBefore(p,msgs)}const composer=$('composer');if(composer&&!$('replyBanner')){const r=document.createElement('div');r.id='replyBanner';r.className='replyBanner';r.innerHTML='<span></span><button>✕</button>';composer.parentElement.insertBefore(r,composer);r.querySelector('button').onclick=clearReply}if(composer&&!$('voiceBtn')){const b=document.createElement('button');b.id='voiceBtn';b.className='iconBtn voiceBtn';b.type='button';b.title='Voice message';b.textContent='🎙️';composer.insertBefore(b,composer.querySelector('#message'));b.onclick=toggleRecord}}
function renderPinned(){const p=$('pinnedBar');if(!p)return;const pins=items.filter(x=>x.data().pinned&&!hidden.has(x.id));if(!pins.length){p.classList.remove('show');return}const x=pins[pins.length-1],m=x.data();p.classList.add('show');p.innerHTML=`📌 <b>Pinned</b> · ${esc((m.text||'').replace(/^\[\[(MEDIA|VOICE)\]\].*$/,'' ).slice(0,110))}`;p.onclick=()=>{const w=[...$('messages').querySelectorAll('.bubbleWrap')][items.findIndex(y=>y.id===x.id)];w?.scrollIntoView({behavior:'smooth',block:'center'})}}
function addTools(){const wraps=[...$('messages').querySelectorAll('.bubbleWrap')];items.forEach((x,i)=>{const w=wraps[i];if(!w)return;w.dataset.featureId=x.id;if(hidden.has(x.id)){w.style.display='none';return}else w.style.display='';const m=x.data();let tools=w.querySelector('.featureTools');if(!tools){tools=document.createElement('div');tools.className='featureTools';w.appendChild(tools)}tools.innerHTML=`<button data-a="reply">↩ Reply</button><button data-a="react">😊 React</button><button data-a="pin">${m.pinned?'📌 Unpin':'📌 Pin'}</button>${m.sender===uid?`<button data-a="deleteMe">🗑️ Delete for me</button><button data-a="deleteAll" class="danger">🗑️ Delete for everyone</button>`:''}<button data-a="history">🕘 History</button>`;let rr=w.querySelector('.reactionRow');if(!rr){rr=document.createElement('div');rr.className='reactionRow';w.appendChild(rr)}const rs=m.reactions||{};rr.innerHTML=Object.entries(rs).filter(([,v])=>Array.isArray(v)&&v.length).map(([e,v])=>`<button class="reactionChip" data-r="${e}">${e} ${v.length}</button>`).join('');tools.onclick=e=>{const b=e.target.closest('button');if(!b)return;featureAction(b.dataset.a,x.id,m,w)};rr.onclick=e=>{const b=e.target.closest('[data-r]');if(b)toggleReaction(x.id,m,b.dataset.r)};if(m.replyTo&&!w.querySelector('.replyQuote')){const q=document.createElement('div');q.className='replyQuote';q.innerHTML=`<b>${esc(m.replyTo.senderName||'Unknown')}</b>${esc(m.replyTo.text||'')}`;const bubble=w.querySelector('.bubble');bubble?.insertBefore(q,bubble.firstChild)}if(m.deleted&&w.querySelector('.bubble')){w.querySelector('.bubble').textContent='This message was deleted';w.querySelector('.bubble').classList.add('deletedMsg')}if(m.voice&&!w.querySelector('.voiceBubble')){const b=w.querySelector('.bubble');if(b){b.innerHTML=`<div class="voiceBubble">🎙️ <audio controls preload="metadata" src="${esc(m.voice.url)}"></audio></div>`}}});renderPinned()}
async function featureAction(a,id,m,w){if(a==='reply'){pendingReply={id,text:m.deleted?'This message was deleted':(m.text||''),senderName:m.senderName||'Unknown'};pendingReplyAt=Date.now();const r=$('replyBanner');r.classList.add('show');r.querySelector('span').textContent=`Replying to ${pendingReply.senderName}: ${pendingReply.text}`;$('message')?.focus()}if(a==='react'){const e=prompt('Reaction: ❤️ 😂 👍 😮 😢 🔥');if(e&&emojis.includes(e))toggleReaction(id,m,e)}if(a==='pin')await updateDoc(doc(db,'chats',path,'messages',id),{pinned:!m.pinned,pinnedBy:uid,pinnedAt:serverTimestamp()});if(a==='deleteMe'){hidden.add(id);saveHidden();w.style.display='none'}if(a==='deleteAll'){if(m.sender!==uid)return;await updateDoc(doc(db,'chats',path,'messages',id),{deleted:true,deletedAt:serverTimestamp(),text:'',reactions:{},voice:null})}if(a==='history')showHistory(m)}
async function toggleReaction(id,m,e){const r={...(m.reactions||{})},arr=Array.isArray(r[e])?[...r[e]]:[],i=arr.indexOf(uid);if(i>=0)arr.splice(i,1);else arr.push(uid);if(arr.length)r[e]=arr;else delete r[e];await updateDoc(doc(db,'chats',path,'messages',id),{reactions:r})}
function clearReply(){pendingReply=null;pendingReplyAt=0;$('replyBanner')?.classList.remove('show')}
function showHistory(m){const modal=document.getElementById('historyModal')||(()=>{const x=document.createElement('div');x.id='historyModal';x.className='historyModal';x.innerHTML='<div class="historyCard"><h3>🕘 Edit history</h3><div id="historyList"></div><button id="closeHistory" class="iconBtn">Close</button></div>';document.body.appendChild(x);x.onclick=e=>{if(e.target===x)x.classList.remove('open')};x.querySelector('#closeHistory').onclick=()=>x.classList.remove('open');return x})();const list=modal.querySelector('#historyList'),h=m.editHistory||[];list.innerHTML=h.length?h.map((x,i)=>`<div class="historyItem"><small>Version ${i+1}${x.at?.toDate?' · '+x.at.toDate().toLocaleString():''}</small>${esc(x.text||'')}</div>`).join(''):'<div class="historyItem">No previous versions recorded.</div>';modal.classList.add('open')}
async function searchMessages(){if(!path)return;const q=(prompt('Search messages in this chat:')||'').trim().toLowerCase();if(!q){items.forEach((x,i)=>{const w=$('messages').querySelectorAll('.bubbleWrap')[i];if(w&&!hidden.has(x.id))w.style.display=''});return}const wraps=[...$('messages').querySelectorAll('.bubbleWrap')];let count=0;items.forEach((x,i)=>{const m=x.data(),hay=JSON.stringify(m).toLowerCase(),w=wraps[i];if(!w)return;const ok=hay.includes(q)&&!hidden.has(x.id);w.style.display=ok?'':'none';if(ok)count++});alert(`${count} message${count===1?'':'s'} found.`)}
let recorder=null,chunks=[];async function toggleRecord(){if(recorder&&recorder.state==='recording'){recorder.stop();return}try{if(!navigator.mediaDevices?.getUserMedia)throw new Error('Voice recording is not supported on this browser.');const stream=await navigator.mediaDevices.getUserMedia({audio:true});chunks=[];recorder=new MediaRecorder(stream);const b=$('voiceBtn');b.classList.add('recording');b.textContent='⏹️';recorder.ondataavailable=e=>e.data.size&&chunks.push(e.data);recorder.onstop=async()=>{b.classList.remove('recording');b.textContent='🎙️';stream.getTracks().forEach(t=>t.stop());const blob=new Blob(chunks,{type:recorder.mimeType||'audio/webm'});try{const media=await uploadVoice(blob);await sendVoice(media)}catch(e){alert('Voice message failed: '+e.message)}};recorder.start();setTimeout(()=>{if(recorder?.state==='recording')recorder.stop()},120000)}catch(e){alert(e.message)}}
function loadGIS(){return new Promise((res,rej)=>{if(window.google?.accounts?.oauth2)return res();const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.onload=res;s.onerror=()=>rej(new Error('Google sign-in service could not load.'));document.head.appendChild(s)})}
async function driveToken(){if(!GOOGLE_CLIENT_ID||GOOGLE_CLIENT_ID.startsWith('PASTE_'))throw new Error('Google Drive is not configured. Add your OAuth Web Client ID first.');if(accessToken)return accessToken;await loadGIS();return new Promise((res,rej)=>{tokenClient=google.accounts.oauth2.initTokenClient({client_id:GOOGLE_CLIENT_ID,scope:DRIVE_SCOPE,callback:r=>r.error?rej(new Error(r.error)):res(accessToken=r.access_token)});tokenClient.requestAccessToken({prompt:'consent'})})}
async function uploadVoice(blob){const token=await driveToken(),name=`Tanmira-Voice-${Date.now()}.webm`;const init=await fetch('https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json','X-Upload-Content-Type':blob.type,'X-Upload-Content-Length':String(blob.size)},body:JSON.stringify({name,mimeType:blob.type})});if(!init.ok)throw new Error('Drive upload could not start.');const session=init.headers.get('Location');const r=await fetch(session,{method:'PUT',headers:{'Content-Length':String(blob.size),'Content-Range':`bytes 0-${blob.size-1}/${blob.size}`},body:blob});if(!r.ok)throw new Error('Drive voice upload failed.');const f=await r.json();const p=await fetch(`https://www.googleapis.com/drive/v3/files/${f.id}/permissions`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({type:'anyone',role:'reader',allowFileDiscovery:false})});if(!p.ok)throw new Error('Could not make the voice playable.');return{id:f.id,url:`https://drive.google.com/uc?export=download&id=${f.id}`,name}}
async function sendVoice(voice){const text='🎙️ Voice message';await setDoc(doc(db,'chats',path),path==='global'?{type:'group',name:'Everyone',lastMessage:text,lastSender:uid,updatedAt:serverTimestamp()}:{type:'personal',members:path.split('_'),lastMessage:text,lastSender:uid,updatedAt:serverTimestamp()},{merge:true});await addDoc(collection(db,'chats',path,'messages'),{text,sender:uid,senderName:nickname,createdAt:serverTimestamp(),edited:false,voice})}
function maybeReplyAndHistory(){const newest=items.slice().reverse().find(x=>x.data().sender===uid&&!x.data().deleted);if(newest&&pendingReply&&pendingReplyAt&&newest.data().createdAt?.toDate?.().getTime()>=pendingReplyAt-3000&&!newest.data().replyTo){updateDoc(doc(db,'chats',path,'messages',newest.id),{replyTo:pendingReply}).catch(()=>{});clearReply()}items.forEach(x=>{const m=x.data(),old=oldTexts.get(x.id);if(m.text!=null&&old!=null&&m.edited&&old!==m.text){const h=Array.isArray(m.editHistory)?m.editHistory:[];if(!h.some(v=>v.text===old)){updateDoc(doc(db,'chats',path,'messages',x.id),{editHistory:[...h,{text:old,at:serverTimestamp()}]}).catch(()=>{})}}if(m.text!=null)oldTexts.set(x.id,m.text)})}
async function subscribe(){const np=await getPath();if(!np||np===path)return;if(unsub)unsub();path=np;hidden=currentHidden();oldTexts.clear();const q=query(collection(db,'chats',path,'messages'),orderBy('createdAt','asc'));unsub=onSnapshot(q,s=>{items=s.docs;maybeReplyAndHistory();setTimeout(addTools,60)},e=>console.debug('features',e))}
function watch(){inject();ensureUI();subscribe();const title=$('title');if(title)new MutationObserver(()=>{path=null;subscribe()}).observe(title,{childList:true,characterData:true,subtree:true});new MutationObserver(()=>addTools()).observe($('messages'),{childList:true,subtree:true})}
setTimeout(()=>{uid=auth.currentUser?.uid||uid;if(uid)watch()},700);
